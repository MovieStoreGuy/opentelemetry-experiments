default:
all_set:
  metrics:
    jenkins.job.commit_delta:
      enabled: true
    jenkins.job.duration:
      enabled: true
    jenkins.jobs.count:
      enabled: true
none_set:
  metrics:
    jenkins.job.commit_delta:
      enabled: false
    jenkins.job.duration:
      enabled: false
    jenkins.jobs.count:
      enabled: false
